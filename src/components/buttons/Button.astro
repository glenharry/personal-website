---
const { title, href, rank, small, large } = Astro.props;
---

<a
  href={href}
  class={`btn btn${rank === "secondary" ? "-secondary" : ""} 
  btn-${small ? "small" : large ? "large" : "small"}`}
>
  {title}
</a>
